func mySqrt(x int) int {
    if x < 2 {
        return x
    }
    l:=2
    r:=x/2
    for l <= r{
        m:=l+(r-l)/2
        if x == m*m{
            return m
        }else if x < m*m{
            r=m-1
        }else{
            l=m+1
        }
    }
    return r
}
